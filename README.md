# Here are your Instructions
好的，按优先级和处理顺序整理成一个清单，可以照着一条一条推：

P0-1｜把注册信息接入 AI 上下文

现状：parent_role、top_concerns 在 main.py:1591-1607 里查出来了，但只喂给了日志用的 _save_normalized_input，从没传进真正生成回复的 _nuri_reply_sync。
为什么优先：改动小、见效快，直接命中 Linda 说的"缺少同理心"——AI 现在真不知道对面是谁。
需要做的：把 context_hints 拼进 _nuri_reply_sync 的 system prompt（跟 memory_ctx 一个位置）。
P0-2｜给对话历史加窗口/摘要机制

现状：main.py:739 每轮把 session 从建立至今的全部消息原样发给 GPT，单一对话框长期用下去历史只会越滚越大。
为什么优先：这是"卡住"的头号嫌疑，也是后面所有优化（包括 Linda 的纠偏记忆）能不能长期稳定工作的地基——不解决，堆的东西越多，模型表现只会越差。
需要做的：定一个窗口策略（比如保留最近 N 条原文 + 更早内容滚动摘要成一段"对话小结"存起来），具体窗口大小和摘要触发时机需要再定。
P1-1｜建 Linda 的纠偏反馈闭环

现状：这是她明确提出的核心诉求——AI 每次都从零开始，她的修改经验存不下来。
需要做的（分两步）：
内部审核流程：Linda 能看到 AI 草稿，按她说的四类（方向错误/太冗长/语气不符/缺同理心或费用建议）标注问题+写修改建议，存进一张新表（参考现有 user_memories 的模式，但是全局的，不挂在单个用户下）。
定期把攒的纠偏人工提炼成 NURI_PERSONA 里的稳定规则——不是无限堆进 prompt，是隔一段时间人工"消化"一次。
建议先跑一两周攒真实案例，不要一上来就想做全自动。
P1-2｜AI 建议任务的卡片从未渲染（之前审查时发现的断点）

现状：AI 判断该建任务时，后端存的 transition.kind 是 "tasks_generated"（main.py:1677），但前端聊天页只认 "task_suggestion"（chat/[id].tsx:258），而且后端也没把生成的任务详情（title/description/steps）放进 transition，只给了个数量 count。
为什么算 AI 交互体验：这是"AI 主动帮你做点什么"这个环节完全不通，用户感觉不到 AI 的主动性/价值。
需要做的：统一 kind 命名，并把生成的任务对象本身塞进 transition 里给前端渲染。
P2｜复现"说几句话就卡住"

建议先让测试人员留意：卡住是不是发生在对话轮次比较多之后（比如第 10+ 轮）。如果是，大概率就是 P0-2 没解决前的直接后果；如果轮次少也会卡，那是另一个独立 bug，需要单独查。
这五条里，P0-1 和 P1-2 都是"找到具体代码位置、改动范围明确"的活，可以随时叫我改；P0-2 和 P1-1 需要先定设计（窗口策略怎么定、纠偏表结构怎么设计），要不要现在就把其中一个的设计方案定下来？